Policy network architecture
```
obs(37)
  -> Linear(37,128) + GELU
  -> LSTM(128,128)
  -> [actor: Linear(128,1), logstd param] -> Normal -> tanh+scale to [-5,5]
  -> [critic: Linear(128,1)]
```


- [x] give (T - T_min)/(T_max - T_min) as obs
- [x] try to train on only two T values $T \in \{ T_{\min}, T_{\max} \}$, instead of a uniform distribution $T \sim E[T_{\min}, T_{\max}]$
- [x] increase model size (OpenAI 5 used a single layer 4096 unit LSTM)
- [x] increase the complexity of the decoder section
> increase obs encoder to 3 layer MLP (GELU in between)
> increased critic decoder head to 3 layer MLP (GELU in between)
> kept actor mean decoder head a single linear layer (+tanh squash at the end)
> kept actor log std a learnable module parameter

> [OpenAI Five ](https://www.researchgate.net/publication/337967587_Dota_2_with_Large_Scale_Deep_Reinforcement_Learning) used
> - complex observation encoder
> - which fed into a single layer 4096 unit LSTM
> 	- then a single linear layer for critic head
> 	- then a single linear layer for each actor head (multiple actions)

> the idea is that the big LSTM learns everything meaningful, then the linear layers just "read out" the needed information

- [x] [in progress] run a sweep with the bigger model size and the horizon incorporated into the observation
- [x] see if performance degrades if we remove window prices, as the LSTM should implicitly keep track of this
	(alternatively, see if the same happens if we remove the LSTM and keep the window prices)

> with price_window_size=32: 
> - https://wandb.ai/leonardotoffalini-e-tv-s-lor-nd-university/pufferlib/runs/8oezaicu?nw=nwuserleonardotoffalini
>
> with price_window_size=1:
> - https://wandb.ai/leonardotoffalini-e-tv-s-lor-nd-university/pufferlib/runs/al26pmwa?nw=nwuserleonardotoffalini
>
> with price_window_size=128:
>  - https://wandb.ai/leonardotoffalini-e-tv-s-lor-nd-university/pufferlib/runs/365jyv8o?nw=nwuserleonardotoffalini

- [ ] run a sweep with price_window_size=1 and compare that with the sweeped 32 for correct comparison

- [ ] if we remove the information of the trading horizon then the only thing it can learn is a hom. strategy

- [x] sweep for the LSTM hidden state size too
- [x] sweep for the price_window_size too
- [x] sweep for the prediction_len too
- [x] make the evaluator of the best policy automatic via a wrapper
- [ ] try to backprop on the actor and critic with different ratios
- [x] fix stalling after a trial in sweep
	> The optimizer is bad, considering switching to optuna
- [ ] see if per T bespoke models give the hoped for asymptotic
	> in progress...
- [ ] if the above is successful we can see if curriculum learning works by leap frogging from smaller to bigger T
	> in progress...
	 - [ ] make the jumps in distribution more gradual
	 - [ ] make the learning rate _smaller_ when jumping distributions
	 - [ ] see what is causing the problem in the first couple M steps in the training to have such highs and lows













- [x] try varying $\pm 2$ around $256$
- [x] try to see if 256 is special or if this works for some other $T$ \[64, 128, 192, 256, 320, 384\]
- [x] do the same thing for $H=0.9$
- [ ] the following would be a strong result: if we restrict to learning only Markov strategies do we get worse results?
	- [ ] MLP with window prices
	- [ ] MLP without window prices
	- [x] LSTM with window prices
	- [ ] LSTM without window prices

- [x] measure the autocovariance of the strategy to see if the current strategy uses the past
$$
\text{Cov} (\phi(t), \; \phi(t - h)) \quad (h=0,\dots,T),
$$
where $\phi$ is the strategy.











- [ ] plot the asymptote of the different policies trained on different horizons with the same config as the best $T=256$
- [ ] see how robust the sweep is by rerunning the same sweep setup and see if we get about the same result from different inits
- [ ] see what the persistent strategies do on sample trajectories plot and on an autocorrelation plot
- [ ] sweep the same way as on T=256 H=0.1 but on H=0.9
- [x] try to see which params were important by binary search excluding them explicitly set and use rational defaults instead
- [x] see if price_window_size actually shows up in the autocorrelation plot by training with the same config but increasing price_window_size and see if the autocorrelation of the action $\phi(t)$ vanishes at the same point where price_window_size is











- [x] change remote url from personal to aielte-research
> done: https://github.com/aielte-research/InvestML/tree/investml

obs vector
- tick/T
- (T - T_min)/(T_max - T_min)
- price
- riskless
- risky
- window prices vector (32 long)
	- price 32 days ago
	- $\quad \vdots$
	- price yesterday


$$
f_{\sigma} (x) \implies N(\mu, \sigma ^{2})
$$

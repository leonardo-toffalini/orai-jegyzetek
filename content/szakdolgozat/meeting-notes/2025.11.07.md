- [x] Run the 1B run with different seeds to see if it bogs down in the same local optima

- [x] Change the 10 ($n$) to different values, see what happens
- [ ] See how to $T / 2$ liquidation ratio changes the outcome


- [x] survey what is wrong with `fbm.h`
	Lorant: inverse fft scaling was off, before `... / N`, after `... / sqrt(N)`

- [ ] log constant optimal performance values to compare against
